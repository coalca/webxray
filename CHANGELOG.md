# 版本记录

## 0.3.2 - 2026-07-29

### Windows 服务卸载

- 卸载 Windows 服务后永久删除整个 `C:\ProgramData\WebXray`。
- 令牌、节点、订阅、Xray、Geo 文件、配置和日志均不保留。
- 卸载失败或数据目录删除失败时返回错误，不再报告虚假的成功状态。
- Windows 原生测试验证服务数据被清空，同时免安装版 `data` 不受影响。

## 0.3.1 - 2026-07-29

### Windows 数据目录

- 免安装版继续把全部数据保存在解压目录的 `data`，移动时可随程序一起带走。
- Windows 服务版改用系统级目录 `C:\ProgramData\WebXray`，程序升级不再覆盖服务数据。
- 两种模式不共享也不自动复制数据，各自生成配置和访问令牌。
- 服务安装会保护数据目录 ACL，仅允许 SYSTEM 和管理员访问。
- 增加 `webxray.cmd -s token`、`-s url` 和 `-s doctor` 服务数据管理命令。

## 0.3.0 - 2026-07-28

### 界面

- 以“后端、核心、活动节点、代理入口”重构主控制台。
- 增加独立移动节点列表，桌面表格只保留必要字段。
- 节点表单按协议显示字段，重做亮色和暗色主题，主按钮达到 WCAG AA 对比度。
- 代理向局域网开放且没有认证时显示安全提示。

### 运行时

- 认证状态接口增加版本、平台、架构和发行形态信息。
- `token`、`url` 和 `doctor` 命令改为可直接读取的纯净输出。
- 新的非 Docker 安装默认只在本机监听 Mixed 代理。

### 发行

- Docker 增加普通代理和 Linux TUN 两套 Compose。
- amd64 和 arm64 Deb 内置 Node.js。
- Windows 分离直接运行、安装服务和卸载服务入口。
- Release 流水线增加 Windows 原生直接运行与服务冒烟测试。

### 质量

- 扩充单元、设计契约、打包契约和 Playwright 核心生命周期测试。
- 增加平台、安装、安全、升级和仓库架构文档。

## 0.2.3 - 2026-07-28

- 修复隐藏 ID 字段覆盖原生表单 ID 导致订阅和节点无法保存的问题。
- 增加持续浏览器回归测试并改进运行时错误提示。
- 将初始红色主色替换为蓝色主色。
