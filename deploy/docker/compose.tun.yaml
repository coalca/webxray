services:
  webxray:
    image: ghcr.io/coalca/webxray:latest
    container_name: webxray
    restart: unless-stopped
    network_mode: host
    cap_add:
      - NET_ADMIN
    devices:
      - /dev/net/tun:/dev/net/tun
    volumes:
      - ./data:/data
